<%# <div class="d-flex flex-column align-items-center buttons-container-initial m-2"> %>
<div class="business-index-button-container">
 <% @businesses.distinct.pluck(:category).each do |category| %>
      <%= button_to category.capitalize.gsub('_', ' ') + "s", meet_businesses_path(params[:meet_id]), method: :get, params: { query: category }, class:"button-business-index", style: "background-image: url(#{asset_path(Business::RESTAURANT_PHOTOS.sample)})" %>
  <% end %>
</div>
